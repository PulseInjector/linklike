# Contributing

```bash
pnpm install
pnpm check
```

Open a pull request with a clear description. Link related issues when applicable.

Run `pnpm check` before you push. It runs lint, typecheck, format, validate on the fixture project, and tests.

Questions belong in GitHub Discussions (when enabled) or issues for actionable work.
